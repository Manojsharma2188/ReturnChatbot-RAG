package com.chatbot.returnchatbot;

import org.junit.jupiter.api.Test;

class ReturnchatbotApplicationTests {

    @Test
    void contextLoads() {
        // Basic smoke test that verifies the test framework works.
        // Full Spring context loading requires Qdrant, PostgreSQL and Ollama
        // to be running, which is not available during build.
        // Integration tests can be run manually with infrastructure in place.
    }
}